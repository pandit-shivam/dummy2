import React from 'react'

const FucntionparesentProps = (props) => {
    return (
        <div>FucntionparesentProps
            <button onClick={props.data}>call data function</button>
        </div>
    )
}

export default FucntionparesentProps